const calculateBattleResult = (attackMode, attackingStats, defendingStats) => {
  let result;
  switch (attackMode) {
    case 'physical':
      break;
    case 'magic':
      break;
  }
};

exports.battle = (playerStats, enemyStats, turnNumber) => {};
